name: abota

# ClickHouse lives in ClickHouse Cloud — this compose file does not start a
# local clickhouse-server. backend/.env must contain CLICKHOUSE_HOST and
# CLICKHOUSE_PASSWORD from the Cloud console (see backend/.env.example).

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: abota_backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      PYTHONDONTWRITEBYTECODE: "1"
      PYTHONUNBUFFERED: "1"
    volumes:
      - ./backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/health')",
        ]
      interval: 10s
      timeout: 5s
      retries: 8
      start_period: 20s

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: abota_frontend
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
    depends_on:
      backend:
        condition: service_healthy

  # One-shot schema + catalog + fact seed against ClickHouse Cloud.
  # Exits 0 when data already exists (safe on every `docker compose up`).
  seed:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: abota_seed
    restart: "no"
    env_file:
      - ./backend/.env
    environment:
      PYTHONDONTWRITEBYTECODE: "1"
      PYTHONUNBUFFERED: "1"
    volumes:
      - ./backend:/app
    command: python -m scripts.seed_clickhouse
    depends_on:
      backend:
        condition: service_healthy

volumes:
  frontend_node_modules:
