# Leave NEXT_PUBLIC_API_URL unset so the browser calls same-origin /api.
# Next.js proxies those requests to FastAPI (BACKEND_INTERNAL_URL).
# NEXT_PUBLIC_API_URL=
BACKEND_INTERNAL_URL=http://127.0.0.1:8000
